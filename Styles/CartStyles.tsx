import {StyleSheet} from 'react-native';

export const CartStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 10,
    paddingVertical: 15,
    backgroundColor: '#fff',
    height: '100%',
  },
  bottom: {bottom: 0, right: 0, left: 0, position: 'absolute', padding: 15},
});
